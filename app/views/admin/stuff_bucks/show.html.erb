<% @page_title = "#{@type.title} 的 #{@buck.school.title} Buck" -%>

<% content_for :sidebar do -%>
  <h4>> <%= link_to "返回管理首页", admin_url %></h4>
  <%= render :partial => "/admin/shared/operation_list" %>
<% end -%>

<div class="box">
  <div class="boxOuter">
    <div class="boxInner">
      <h2><%= h @type.title %> 的 <%= h @buck.school.title %> Buck</h2>
      <div class="contents">
        <p>总共 <%= @stuffs.size %> 个, <%= link_to "下载密码CSV文件", formatted_admin_stuff_type_buck_url(@type, @buck, :format => :csv ) %>, <%= link_to "返回", admin_stuff_type_bucks_url(@type) %></p>
        <table>
          <tr>
            <th>password</th>
            <th>user</th>
            <th>school</th>
          </tr>
          <% @stuffs.each do |b| -%>
          <tr>
            <td><%= b.code %></td>
            <td><%= b.user.login if b.user %></td>
            <td><%= b.school.title if b.school %></td>
          </tr>
          <% end -%>
        </table>
      </div>
    </div>
  </div>
</div>