<% @page_title = "修改商品 - 商城管理" -%>

<% content_for :sidebar do -%>

  <%= render :partial => "/admin/public/operation_list" %>
<% end -%>

<div class="box">
  <div class="boxOuter">
    <div class="boxInner">
      <h3>修改商品</h3>
      <div class="contents">
        <% form_for :good, :url => admin_good_url(@good), :html => {:method => "put"} do |f| %>
          <%= render :partial => "form", :object => f %>
          <%= submit_tag "修 改" %> 或 <%= link_to "返回", admin_goods_url %>
        <% end %>
      </div>
    </div>
  </div>
</div>