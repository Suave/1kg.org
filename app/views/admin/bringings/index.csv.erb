通过状态 申请人 学校 申请月份 盒子 申请原因 困难预估 真实姓名 地址邮编 联系电话
<% @executions.each do |e| -%>"<%= e.state %>" "<%= e.user.login %>" "<%= e.school.title %>" "<%= "#{e.start_at.year}年#{e.start_at.month}月" %>" "<%= e.bringings.map{|b| "#{b.box.name}[#{b.number}]、"}%>" "<%= e.reason %>" "<%= e.problem %>" "<%= e.realname %>" "<%= e.address %>" "<%= e.telephone %>"
<% end -%>
