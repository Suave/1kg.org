申请时间;通过状态;申请人;学校;使用月份;<%= @boxes.map{|b| "\"#{b.name}\";"} %>申请人姓名;申请人联系电话;申请人邮箱;收货人＆联系方式;收货地址邮编;签收单位;申请原因;困难预估;反馈数
<% @executions.each do |e| -%>"<%= e.created_at.to_date %>";"<%= e.state_tag %>";"<%= e.user.login %>";"<%= e.school.title %>";"<%= "#{e.start_at.year}年#{e.start_at.month}月" %>";<%= @boxes.map{|b| (r = Bringing.find(:first,:conditions => {:box_id => b.id,:execution_id => e.id})) ? "#{r.number};": "0;" }%>"<%= e.realname %>";"<%= e.telephone %>";"<%= e.email %>";"<%= e.consignee %>";"<%= e.address %>";"<%= e.receipient %>";"<%= e.reason.gsub("\r\n",' ') %>";"<%= e.problem.gsub("\r\n",' ') %>";"<%= e.topics_count %>" 
<% end -%>
