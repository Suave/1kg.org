<div class="box">
  <div class="boxOuter">
    <div class="boxInner">
      <h2>请选择要捐赠的学校</h2>
      <div class="contents">
        <p>您的积分可以兑换<%= @donation.product_number %>个<%= @requirement_type.title %>，请在下面需要<%= @requirement_type.title %>的学校中选择一所您想要捐赠的学校：</p>
        <table>
          <tr>
            <th>学校</th>
            <th>需求数量</th>
            <th>已捐赠</th>
            <th>&nbsp;</th>
          </tr>
          <% @requirements.each do |r| -%>
          <tr>
            <td>
              <%= link_to r.school.title, school_url(r.school), :target => "_blank" %>
              <%= "<br />#{b.for_team_tip}" if r.for_team? %>
            </td>
            <td><%= h r.quantity %></td>
            <td><%= h r.matched_count %></td>
            <td>
              <%= link_to "我要捐给这所学校", donation_path(@donation, :requirement_id => r.id), :method => 'put' %>
            </td>
          </tr>
          <% end -%>
        </table>
      </div>
    </div>
  </div>
</div>