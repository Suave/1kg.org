<% groups.each do |g| %>
<dl class="obu">
  <dt><%= link_to avatar_for(g, "large"), group_url(g) %></dt>
  <dd>
    <%= link_to g.title, group_url(g) %>
    (<%= g.memberships.count %>)
  </dd>
</dl>
<% end -%>
<style type="text/css">dd{height:58px;padding:2px} </style>