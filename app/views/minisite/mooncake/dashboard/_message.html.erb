<% if @stuff -%>
  <div class="avatar"><%= mooncake_avatar_for stuff.user, "large" %></div>
  <div class="content">
    <p class="meta">
      <%= link_to stuff.user.login, user_url(stuff.user) %> 写于 <%= stuff.matched_at.to_date %>
    </p>
    <div class="comment-body"><%= stuff.comment_html %></div>
  </div>
<% else -%>
  <p>感谢您购买时光之秋公益月饼</p>
<% end -%>