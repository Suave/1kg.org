<div>
  <%= main_photo_thumb(@community) -%>       
  <div class="school_list_meta">
    <h3><%= link_to "#{h(@community.title)}",  project_execution_url(@execution.project,@execution), :target => "_blank" %></h3>
    <p>
      <span class="gary_span">
        <% if @community.geo.parent %>
        <%= h @community.geo.parent.name %>
        <% end %>
        <%= h @community.geo.name %>
      </span><br/>
      <span>项目状态: <%= state_tag(@execution.state)%></span>
    </p>
  </div>
</div>
