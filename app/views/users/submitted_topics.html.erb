<% @page_title = @title = "#{@user.login}发起的话题" -%>

<% content_for :sidebar do -%>
  <div class="cleanfix" style="clear:both;height:20px"></div> 
<span class="remark"><%= link_to raw("&raquo; 回到#{@user.login}的空间"),user_url(@user)%></span>
<% end %>

<div class="newbox">
      <div class="contents">
      <%= render :partial => "/topics/topics", :locals => {:topics => @topics} unless @topics.blank? %>
      <%= customize_paginate @topics %>
      </div>
</div>
