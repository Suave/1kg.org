<% @page_title = @title = "#{h @user.login}参加的小组" %>

<% content_for :fullsize do -%>
  <div class="box">
    <div class="boxOuter">
      <div class="boxInner">
        <h3></h3>
        <span class="postNew"><%= link_to "返回#{@user.login}的空间", user_url(@user) %></span>
        <div class="contents clearfix">
          <%= render :partial => "/groups/groups", :object => @groups %>
        </div>
        <p class="remark"><%= link_to "返回#{@user.login}的空间", user_url(@user) %></p>
      </div>
    </div>
  </div>
<% end -%>